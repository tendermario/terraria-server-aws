import * as stopLambda from '../lib/lambdas/stop-lambda'

test('EC2 Instance Stopped', async () => {
  // const result = await stopLambda.handler()

  // expect(result.statusCode).toBe(200)
  // expect(JSON.parse(result.body).result).toBe('stopping')
})