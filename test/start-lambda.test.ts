import * as startLambda from '../lib/lambdas/start-lambda'

test('EC2 Instance Started', async () => {
  // const result = await startLambda.handler()

  // expect(result.statusCode).toBe(200)
  // expect(JSON.parse(result.body).result).toBe('pending')
})