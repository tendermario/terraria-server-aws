import * as statusLambda from '../lib/lambdas/status-lambda'

test('Retrieve EC2 Instance Status', async () => {
  // const result = await statusLambda.handler()

  // expect(result.statusCode).toBe(200)
  // expect(JSON.parse(result.body).result).toBe('stopped')
})